plugins:
  schedulers:
    - 'lib.scheduler.SlurmScheduler'
    - 'lib.scheduler.PBSScheduler'
    - 'lib.scheduler.OSScheduler'

  services:
    - 'lib.service.TurboVNCServer'
    - 'lib.service.Fake'
  