defaults:
  TOP:
    SCHEDULER :
      Slurm :

        substitutions :
          HEADER: |
            #!/bin/bash
            #SBATCH --time=@{QUEUE.TIME}
            #SBATCH --job-name=$RCM_SESSIONID
            #SBATCH --output $RCM_JOBLOG
            #SBATCH -A @{ACCOUNT}
            #SBATCH --partition=@{QUEUE}
            #
